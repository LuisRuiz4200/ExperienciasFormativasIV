@model IEnumerable<Dominio.Entidad.TecnicoModel>

@{
    ViewBag.Title = "listarTecnico2";
    Layout = "~/Views/SolicitudRepuesto/registrarSolicitudRepuesto.cshtml";
}

<h2>listarTecnico2</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cod_tecnico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nom_tecnico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ape_tecnico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado_tecnico)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cod_tecnico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nom_tecnico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ape_tecnico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado_tecnico)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
